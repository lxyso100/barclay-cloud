server:
  port: 8090

spring:
  application:
    name: provider
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8089/eureka/

