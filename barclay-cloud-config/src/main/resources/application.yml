server:
  port: 8088

spring:
  application:
    name: config
  cloud:
      config:
        server:
          git:
            uri:  https://gitee.com/barclay100/barclay/
            search-paths: spring-cloud-config/*


